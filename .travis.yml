language: minimal

stages:
  - name: lint
  - name: deploy
    if: tag =~ ^\d+\.\d+\.\d+$

jobs:
  include:
    - stage: lint
      os: osx
      language: swift
      install: gem install cocoapods
      script:
        - pod lib lint
    - stage: deploy
      os: osx
      language: swift
      install: gem install cocoapods
      script:
        - pod trunk push --allow-warnings
      after_failure:
        - echo "TRAVIS_TAG: $TRAVIS_TAG"
        - echo "PODSPEC_VERSION: $(./get_podspec_version.sh aBlinkingLabel.podspec)"
