language: objective-c
install:
- gem install cocoapods
script: true

stages:
  - lint
  - deploy

jobs:
  include:
    - name: lint
      script:
        - pod spec lint
    - name: deploy
      script:
        - pod trunk push
